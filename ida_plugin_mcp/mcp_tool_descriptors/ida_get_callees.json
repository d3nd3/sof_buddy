{
  "name": "ida_get_callees",
  "description": "Instruction-level callees. Default max_results=80; use max_results=0 for all. Plugin v2+ enforces the cap.",
  "arguments": {
    "type": "object",
    "properties": {
      "symbol_or_address": {
        "title": "Symbol Or Address",
        "type": "string"
      },
      "max_results": {
        "default": 80,
        "title": "Max Results",
        "type": "integer"
      }
    },
    "required": ["symbol_or_address"],
    "title": "ida_get_calleesArguments"
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "result": {
        "title": "Result",
        "type": "string",
        "description": "Markdown: Count (total), optional Returned (after cap), callee list."
      }
    },
    "required": ["result"],
    "title": "ida_get_calleesOutput"
  }
}
