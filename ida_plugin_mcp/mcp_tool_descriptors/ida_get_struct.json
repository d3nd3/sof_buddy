{
  "name": "ida_get_struct",
  "description": "Read-only struct layout: member offsets and type strings (JSON in markdown).",
  "arguments": {
    "type": "object",
    "properties": {
      "struct_name": { "type": "string", "title": "Struct Name" }
    },
    "required": ["struct_name"],
    "title": "ida_get_structArguments"
  }
}
