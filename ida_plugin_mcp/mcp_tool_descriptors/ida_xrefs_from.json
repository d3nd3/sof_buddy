{
  "name": "ida_xrefs_from",
  "description": "Outgoing xrefs from an instruction head. ea: hex or symbol. max_results 0 = unlimited (plugin-capped).",
  "arguments": {
    "type": "object",
    "properties": {
      "ea": { "type": "string", "title": "Ea" },
      "max_results": { "type": "integer", "default": 300, "title": "Max Results" }
    },
    "required": ["ea"],
    "title": "ida_xrefs_fromArguments"
  }
}
